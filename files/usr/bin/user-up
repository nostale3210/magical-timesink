#!/usr/bin/env bash

set -oue pipefail

# update flatpaks
flatpak update -y
flatpak uninstall --unused -y

# update nix packages
nx profile upgrade --all

# update distrobox containers
distrobox upgrade -a
